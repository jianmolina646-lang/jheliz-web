# Django
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost,jhelizservicestv.xyz,www.jhelizservicestv.xyz

# Base URL
SITE_URL=http://127.0.0.1:8000

# Database (SQLite for local dev, Postgres for prod)
DATABASE_URL=sqlite:///db.sqlite3

# Email (use console backend in dev, SMTP en prod)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Para producción con SMTP (ej. Brevo/SendGrid/Hostinger):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp-relay.brevo.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Jheliz <no-reply@jhelizservicestv.xyz>
SUPPORT_ADMIN_EMAIL=admin@jhelizservicestv.xyz

# Mercado Pago (Perú)
MERCADOPAGO_ACCESS_TOKEN=
MERCADOPAGO_PUBLIC_KEY=
MERCADOPAGO_WEBHOOK_SECRET=

# Contact / Social
WHATSAPP_NUMBER=+51999999999
TELEGRAM_USERNAME=jhelizbot

# Bot de Telegram (opcional)
# Token desde @BotFather (/newbot)
TELEGRAM_BOT_TOKEN=
# Tu chat_id numérico para recibir notificaciones de pedidos nuevos
# (obtenlo con @userinfobot en Telegram)
TELEGRAM_ADMIN_CHAT_ID=
# Token random para validar el webhook de Telegram (path + header).
# Genéralo con: python -c 'import secrets;print(secrets.token_urlsafe(32))'
TELEGRAM_WEBHOOK_SECRET=
# Canal público de DISTRIBUIDORES (precios mayoristas, alertas técnicas).
# Puede ser @username o un ID numérico. Vacío = desactivado.
TELEGRAM_CHANNEL_ID=
# Canal público de CLIENTES FINALES (precios cliente, promos, restocks).
# Puede ser @username o un ID numérico. Vacío = desactivado.
TELEGRAM_CUSTOMER_CHANNEL_ID=

# ---------------------------------------------------------------------------
# Seguridad
# ---------------------------------------------------------------------------
# Cifrado de credenciales entregadas (Fernet). Generar UNA vez con:
#   python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
# y guardarla en un gestor de secretos. Si la pierdes, NO podrás descifrar
# las credenciales ya guardadas.
FIELD_ENCRYPTION_KEY=

# django-axes: cuántos intentos fallidos antes de bloquear (por IP+usuario)
# y cuántas horas dura el bloqueo.
AXES_FAILURE_LIMIT=5
AXES_COOLOFF_TIME_HOURS=1

# 2FA en el admin. Déjalo en False hasta que registres tu dispositivo TOTP
# en /jheliz-admin/otp_totp/totpdevice/. Activarlo bloquea el admin a quien
# no tenga 2FA confirmado.
ADMIN_2FA_ENFORCED=False

# IP allowlist al admin. Lista separada por comas de IPs o CIDRs autorizados
# a usar /jheliz-admin/*. Si está vacía, no hay restricción.
# Ejemplos:
#   ADMIN_IP_ALLOWLIST=203.0.113.42
#   ADMIN_IP_ALLOWLIST=203.0.113.0/24,2001:db8::/32
ADMIN_IP_ALLOWLIST=
# Modo soft: en vez de bloquear con 403, deja pasar y solo loguea + alerta a
# Telegram. Útil para validar la lista antes de activar el modo hard.
ADMIN_IP_ALLOWLIST_SOFT=False

# Sentry (monitoreo de errores). Crear cuenta gratuita en https://sentry.io,
# pegar el DSN aquí. Si está vacío, Sentry no se inicializa.
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.05
SENTRY_RELEASE=
